# イラスト導入 実装プラン

## 全体フロー

```
1. プロンプト生成 (5分)
   ↓
2. Antigravity で試作 (30分)
   ※ 5-10個生成して品質確認
   ↓
3. プロンプト調整 (30分)
   ※ 統一感、可愛さ、識別性を確認
   ↓
4. 本番生成 (2-3時間)
   ※ 全100個をGeminiで生成
   ↓
5. ファイル配置 (10分)
   ↓
6. コード実装 (1時間)
   ↓
7. テスト・デプロイ (30分)
```

---

## 詳細手順

### Phase 1: プロンプト生成 ✅

```bash
# プロンプトファイルを生成
npx tsx scripts/generate-prompts.ts

# 生成されるファイル:
# - scripts/image-prompts.json
# - scripts/image-prompts.csv
```

**成果物**
- 100個の動物用プロンプト（JSON/CSV）

---

### Phase 2: 試作生成（Antigravity）

#### 2-1. Antigravity Chat で実行

1. **Claude Desktop で Antigravity を起動**

2. **最初の5個を試作**
   ```
   プロンプト例（Lion）:
   Create a cute, minimalist illustration of a Lion in MBTI personality test style.
   Style requirements:
   - Simple, flat design with rounded shapes
   - Friendly and approachable character
   - Clean lines, no complex details
   - Solid color background matching #FFD700
   - Character should be centered and take up 70% of canvas
   - Kawaii/cute aesthetic similar to Japanese character design
   - No text or labels
   - Square format (1:1 aspect ratio)

   The Lion should look: リーダーシップ, 勇敢, 強い

   Keep it simple, charming, and instantly recognizable.
   ```

3. **生成結果を確認**
   - [ ] 統一感があるか
   - [ ] 動物が識別できるか
   - [ ] 色が適用されているか
   - [ ] 可愛いか・親しみやすいか

#### 2-2. プロンプト調整（必要に応じて）

**よくある調整ポイント**
```typescript
// もっとシンプルにしたい場合
"Minimalist, geometric design with basic shapes"

// もっと可愛くしたい場合
"Super kawaii, chibi character style"

// 色をもっと強調したい場合
"Bold use of {color} as the primary color"

// キャラクターを大きくしたい場合
"Character should take up 80% of canvas"
```

---

### Phase 3: 本番生成

#### オプションA: Antigravity で順次生成（推奨）

```typescript
// scripts/batch-generate.md に記載

1. image-prompts.json を10個ずつに分割
2. Antigravity Chat で順番に実行
3. 生成された画像を public/images/animals/ に保存
   - lion.png
   - tiger.png
   - ...
```

**利点**
- 無料で生成可能
- 途中で品質確認・調整できる
- Antigravity の MCP 機能で自動保存も可能

**欠点**
- 手動操作が必要
- 時間がかかる（100個で2-3時間）

#### オプションB: Google AI Studio（代替案）

```bash
# Google AI Studio の Batch API を使用
# image-prompts.csv をアップロード
# 一括生成・ダウンロード
```

**利点**
- 自動化可能
- 速い（並列生成）

**欠点**
- APIキー設定が必要
- 途中確認ができない

---

### Phase 4: ファイル配置

```bash
# ディレクトリ作成
mkdir -p public/images/animals

# 画像ファイルを配置
public/images/animals/
  ├── lion.png
  ├── tiger.png
  ├── bear.png
  └── ... (100個)

# ファイル名は animals.ts の id と一致させる
```

**画像仕様**
- フォーマット: PNG（透過 or 単色背景）
- サイズ: 512x512 または 1024x1024
- 最適化: TinyPNG などで圧縮推奨

---

### Phase 5: コード実装

#### 5-1. animals.ts に imagePath を追加

```bash
# 自動追加スクリプトを実行
npx tsx scripts/add-image-paths.ts
```

または手動で：
```typescript
// lib/animals.ts

export interface Animal {
  // ... 既存
  emoji: string;
  imagePath?: string;  // ← 追加
}

// 各動物に追加
{
  id: 'lion',
  // ...
  emoji: '🦁',
  imagePath: '/images/animals/lion.png',  // ← 追加
}
```

#### 5-2. コンポーネントを更新

**結果ページ (app/result/page.tsx)**
```typescript
import { AnimalIcon } from '@/components/AnimalIcon';

// 絵文字表示部分を置き換え
<AnimalIcon animal={animal} size="xl" />
```

**動物詳細ページ (app/animal/page.tsx)**
```typescript
import { AnimalIcon } from '@/components/AnimalIcon';

<AnimalIcon animal={animal} size="xl" />
```

#### 5-3. next.config.js の確認

```javascript
// next.config.js
module.exports = {
  images: {
    // 画像最適化の設定（必要に応じて）
    formats: ['image/webp', 'image/avif'],
  },
};
```

---

### Phase 6: テスト

```bash
# 開発サーバー起動
npm run dev

# 確認項目
- [ ] イラストが正しく表示される
- [ ] 画像がない動物は絵文字が表示される（フォールバック）
- [ ] 画像の読み込み速度
- [ ] モバイルでの表示
- [ ] 全100種類が正しく表示される
```

---

### Phase 7: デプロイ

```bash
# ビルドテスト
npm run build

# エラーがなければデプロイ
git add .
git commit -m "Add custom animal illustrations"
git push
```

---

## コスト・時間の見積もり

| 項目 | 時間 | コスト |
|-----|------|--------|
| プロンプト生成 | 5分 | 無料 |
| 試作（5-10個） | 30分 | 無料 |
| プロンプト調整 | 30分 | 無料 |
| 本番生成（100個） | 2-3時間 | 無料（Gemini） |
| ファイル配置 | 10分 | 無料 |
| コード実装 | 1時間 | 無料 |
| テスト | 30分 | 無料 |
| **合計** | **5-6時間** | **無料** |

---

## リスク管理

### リスク1: 品質のバラつき
**対策**: 試作段階で10個生成し、プロンプトを調整

### リスク2: 動物の識別が難しい
**対策**: プロンプトに動物の特徴を明記（例: "with distinctive mane"）

### リスク3: 統一感がない
**対策**: ベースプロンプトを固定し、動物名だけ変更

### リスク4: ファイルサイズが大きい
**対策**: TinyPNG で圧縮（70-80%削減可能）

---

## 段階的導入プラン（推奨）

完璧主義にならず、段階的に進める：

### Step 1: 主要20種だけイラスト化
- 人気の高い動物（ライオン、猫、犬など）
- 診断結果で出やすい動物

### Step 2: フィードバック収集
- ユーザーの反応を確認
- スタイルの調整が必要か判断

### Step 3: 残り80種を生成
- Step 1 で確立したスタイルで一気に生成

---

## 次のアクション

```bash
# 1. プロンプト生成（今すぐ実行可能）
npx tsx scripts/generate-prompts.ts

# 2. image-prompts.json を確認

# 3. Antigravity で最初の5個を試作
#    → image-prompts.json の最初の5個をコピー&ペースト

# 4. 生成結果を確認して、次へ進むか判断
```

**準備完了！あとは実行するだけです 🚀**
